name: hive_preferences
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: 1.9.0
  flutter_test:
    sdk: flutter

dependency_overrides:
  pedantic: 1.9.0

flutter:
  uses-material-design: true
